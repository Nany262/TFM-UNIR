import { Then, When } from "@badeball/cypress-cucumber-preprocessor";
import { LoginPage } from "../page-object/login.page";

const loginPage = new LoginPage();

When("I add an indicator", () => {
});

When("I delete an indicator", function () {

  });

Then("I expect to see the indicator added to the rubric", () => {

});

Then("I expect not see the indicator deleted from the rubric", function () {

  });


  When("I save the information on my rubric", () => {
  });
  
  When("I send the information on my rubric", function () {
  
    });
  
  Then("I expect to see the information saved before", () => {
  
  });
  
  Then("I expect to see a confirmation message", function () {
  
    });

    Then("My coordinator can see the grades", function () {

    });

    Then("I reload the page", function () {

    });

